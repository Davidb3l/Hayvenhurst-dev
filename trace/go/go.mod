module github.com/hayvenhurst/hayven-trace

// Pure stdlib — zero third-party dependencies. The toolchain floor is the
// minimum that ships runtime/pprof's profile parsing surface we rely on.
go 1.21
