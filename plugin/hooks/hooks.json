{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/scripts/install-hayven.sh\" --check 2>&1 || true"
          },
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/scripts/ensure-daemon.sh\" 2>&1 || true"
          }
        ]
      }
    ]
  }
}
